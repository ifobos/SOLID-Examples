import Foundation

public class ParseHandler {
    
    public init(){}
    
    func parse(data: Data) -> [String] {
        // parse the data and create the array
        print("Data parsed to Array of Strings")
        return []
    }
}
